{
  "name": "ai-chatbot-project",
  "version": "1.0.0",
  "private": true,
  "description": "AI Chatbot application built with Next.js and PostgreSQL",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start"
  },
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
