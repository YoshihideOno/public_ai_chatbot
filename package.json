{
  "name": "ai-chatbot-project",
  "version": "1.0.0",
  "private": true,
  "description": "AI Chatbot application built with Next.js and PostgreSQL",
  "scripts": {
    "dev": "cd ai_chatbot_app && npm run dev",
    "build": "cd ai_chatbot_app && npm run build",
    "start": "cd ai_chatbot_app && npm run start",
    "install": "cd ai_chatbot_app && npm install"
  },
  "workspaces": [
    "ai_chatbot_app"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
